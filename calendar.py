# Python Program to display the calendar of a given month of the year

import calendar
yy = int(input("Enter the Year : "))
mm = int(input("Enter the Month : "))

#display the user
print(calendar.month(yy,mm))
